credentials:
  usernames:
    admin:
      email: admin@example.com
      name: 管理者
      password: ""          # 8文字以上・大文字・小文字・数字・記号を含む強いパスワードを設定してください
      role: admin

cookie:
  expiry_days: 1            # セッション有効期間（日数）。共用PCでは短めに設定してください
  key: ""                   # 必ず長いランダム文字列に変更してください（例: python -c "import secrets; print(secrets.token_hex(32))"）
  name: rag_chatbot_auth
